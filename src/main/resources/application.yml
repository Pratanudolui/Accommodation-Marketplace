spring:
  application:
    name: accommodation-marketplace
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${DATASOURCE_URL:jdbc:mysql://localhost:3306/quickstart_db}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:Dolui@2021}
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false